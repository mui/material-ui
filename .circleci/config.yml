# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9

    working_directory: ~/repo

    steps:
      - run:
          name: "Show npm and node versions"
          command: |
            node --version
            npm --version

      - run: yarn docs:depoly

      - checkout

      - restore_cache:
          keys:
          - yarn-packages-{{ .Branch }}-{{ checksum "yarn.lock" }}
          - yarn-packages-{{ .Branch }}
          - yarn-packages-

      # include devDependencies
      - run: yarn install --production=false

      - save_cache:
          name: Save Yarn Package Cache
          key: yarn-packages-{{ .Branch }}-{{ checksum "yarn.lock" }}
          paths:
            - node_modules/

      - run: yarn docs:build

      - run: yarn docs:export

      - persist_to_workspace: 
          root: ~/repo
          paths:
            - .

  test:
    docker:
      - image: circleci/node:8.9

    working_directory: ~/repo

    steps:
      - attach_workspace:
          at: ~/repo

      - run:
          name: Test
          command: yarn test

      - persist_to_workspace: 
          root: ~/repo
          paths:
            - .
  
  # workaround for tag filtering that uses OR instead of AND
  stage_deploy:
    docker:
      - image: circleci/node:8.9

    working_directory: ~/repo
      - run:
          command: echo 'Allow only master branch'

  deploy:
    docker:
      - image: circleci/node:8.9

    working_directory: ~/repo

    steps:
      - attach_workspace:
          at: ~/repo

      - run:
          name: Disable host checking
          command: 

      # deploy docs
      - run: yarn docs:deploy:ghpages

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - build:
          filters:
            branches:
              only: 
                - develop
                - master
      - test:
          requires:
            - build
          filters:
            branches:
              only: 
                - develop
                - master
      - stage_deploy: 
          requires:
            - build
            - test
          filters:
            branches:
              only: 
                - master

      # only deploy tagged commits
      - deploy:
          requires:
            - build
            - test
            - stage_deploy
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /[0-9]+(\.[0-9]+)+(\-cake)+(\.[0-9]+)/