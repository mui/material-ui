// @flow weak

import React, { PropTypes } from 'react';
import Switch from 'material-ui/Switch';
 
export default function Toggle(props, context) {
  return (
      <Switch {...props} isform/>
  );
}
