/* import { expect } from 'chai';

TODO

describe('custom matchers', () => {
  describe('toErrorDev()', () => {
    it('throws if an expected console.error call wasnt recorded', () => {
      expect(() => {
        expect(() => {}).toErrorDev('expected message');
      }).to.throw('asd');
    });

    it('hints at possible previous error calls', () => {
      console.error('unexpected message');

      expect(() => {
        expect(() => {}).toErrorDev('expected message');
      }).to.throw('asd');
    });
  });
}); */
