import * as React from 'react';
import { space } from 'styled-system';

export default function StyledSystemSpaces() {
  const result = space({
    theme: {},
    p: [1, 2, 3],
  });

  return null;
}
