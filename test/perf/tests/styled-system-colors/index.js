import * as React from 'react';
import { color } from 'styled-system';

export default function StyledSystemColors() {
  const result = color({
    theme: {},
    bg: ['red', 'blue'],
  });;

  return null;
}
