import { dialogClasses } from '@org/ui/material/Dialog';

('& .MuiDialog-paperScrollBody');
('& .MuiDialog-paperScrollPaper');

`& .${dialogClasses.paperScrollBody}`;
`& .${dialogClasses.paperScrollPaper}`;
