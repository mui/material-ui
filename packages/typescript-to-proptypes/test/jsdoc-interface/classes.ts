export interface Classes {
  /**
   * root description
   */
  root: string;
  /**
   * slot description
   */
  slot: string;
}

const classes: Classes = { root: 'root', slot: 'slot' };

export default classes;
