const breakpointValues = {
  xs: 0,
  /* 
    doclySm does not exist in MUI and needs to be added on a
    component level for each component that needs to use it
  */
  doclySm: 370,
  sm: 600,
  md: 960,
  lg: 1280,
  xl: 1920,
};

export default breakpointValues;
