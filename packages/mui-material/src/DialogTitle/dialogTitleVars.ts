/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * DialogTitle density token identities — block/inline padding (`16px 24px`),
 * over the agnostic `--comp-*` seams. `private_*` per the density RFC.
 */
export const private_dialogTitleVars = {
  blockPad: '--DialogTitle-blockPad',
  inlinePad: '--DialogTitle-inlinePad',
} as const;
