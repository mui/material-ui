/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * Step density token identity — the horizontal step gutter (`paddingLeft`/
 * `paddingRight` 8px on first/last steps), over `--comp-inlinePad`. `private_*`.
 */
export const private_stepVars = {
  inlinePad: '--Step-inlinePad',
} as const;
