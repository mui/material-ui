import * as React from 'react';
import MenuItem from '@mui/material/MenuItem';

// `value`
<MenuItem value={123} />;
<MenuItem value={'Hello World'} />;
<MenuItem value={{ id: 1, message: 'hello' }} />;
