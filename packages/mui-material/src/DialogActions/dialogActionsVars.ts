/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * DialogActions density token identity — the action-bar padding (8), over
 * `--comp-pad`. The inter-action gap stays literal. `private_*` per the RFC.
 */
export const private_dialogActionsVars = {
  pad: '--DialogActions-pad',
} as const;
