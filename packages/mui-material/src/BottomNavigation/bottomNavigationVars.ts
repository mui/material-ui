/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * BottomNavigation density token identity — the bar height (56, raw px per
 * preset), over `--comp-height`. `private_*` per the density RFC.
 */
export const private_bottomNavigationVars = {
  height: '--BottomNavigation-height',
} as const;
