/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * BottomNavigationAction density token identity — the inline padding (`0 12px`),
 * over `--comp-inlinePad`. `private_*` per the density RFC.
 */
export const private_bottomNavigationActionVars = {
  inlinePad: '--BottomNavigationAction-inlinePad',
} as const;
