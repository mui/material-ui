/* eslint-disable @typescript-eslint/naming-convention, import/prefer-default-export */
/**
 * Breadcrumbs density token identity — the separator's inline gap (8px each
 * side today), over the agnostic `--comp-separatorGap` seam. `private_*` per the
 * density RFC.
 */
export const private_breadcrumbsVars = {
  separatorGap: '--Breadcrumbs-separatorGap',
} as const;
