import TwoWheelerOutlined from './TwoWheelerOutlined';

if (process.env.NODE_ENV !== 'production') {
  console.warn(
    [
      'Material-UI: MotorcycleOutlined icon has been replaced with TwoWheelerOutlined.',
      "Please change your import to `import MotorcycleOutlined from '@material-ui/icons/TwoWheelerOutlined';`",
    ].join(' '),
  );
}

export default TwoWheelerOutlined;
