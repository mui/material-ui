import { GlobalProps } from '@material-ui/styled-engine';
import { Omit } from '@material-ui/types';
/**
 *
 * API:
 *
 * - [Global API](https://material-ui.com/api/global/)
 */
export default function Global(props: Omit<GlobalProps, 'defaultTheme'>): React.ReactElement;
