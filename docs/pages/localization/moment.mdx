import Ad from '_shared/Ad';
import Example from '_shared/Example';
import PageMeta from '_shared/PageMeta';
import * as MomentExample from './Moment.example';

<PageMeta title="Moment localization - @material-ui/pickers" />

## Localization with moment

Moment localization is relying on the global moment object used.

<Ad />

It is also possible to pass a configured global moment with selected locale, default timezone, etc.
Pass the selected locale as a string to the provider to make pickers rerender automatically on
locale change.

#### Example

*Note*: Any picker in the component tree will be rerendered automatically on locale change

<Example source={MomentExample} />
