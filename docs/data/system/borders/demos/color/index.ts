import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BorderColor from './BorderColor';
import ClientProvider from './client';

export default createDemo(import.meta.url, BorderColor, { ClientProvider });
