import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LineHeight from './LineHeight';
import ClientProvider from './client';

export default createDemo(import.meta.url, LineHeight, { ClientProvider });
