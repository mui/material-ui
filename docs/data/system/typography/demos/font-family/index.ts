import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FontFamily from './FontFamily';
import ClientProvider from './client';

export default createDemo(import.meta.url, FontFamily, { ClientProvider });
