import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import OffsetGrid from './OffsetGrid';
import ClientProvider from './client';

export default createDemo(import.meta.url, OffsetGrid, { ClientProvider });
