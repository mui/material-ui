import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicGrid from './BasicGrid';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicGrid, { ClientProvider });
