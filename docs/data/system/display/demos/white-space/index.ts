import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import WhiteSpace from './WhiteSpace';
import ClientProvider from './client';

export default createDemo(import.meta.url, WhiteSpace, { ClientProvider });
