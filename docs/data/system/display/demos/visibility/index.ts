import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import Visibility from './Visibility';
import ClientProvider from './client';

export default createDemo(import.meta.url, Visibility, { ClientProvider });
