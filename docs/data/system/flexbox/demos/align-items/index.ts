import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import AlignItems from './AlignItems';
import ClientProvider from './client';

export default createDemo(import.meta.url, AlignItems, { ClientProvider });
