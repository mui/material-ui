import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import RowAndColumnGap from './RowAndColumnGap';
import ClientProvider from './client';

export default createDemo(import.meta.url, RowAndColumnGap, { ClientProvider });
