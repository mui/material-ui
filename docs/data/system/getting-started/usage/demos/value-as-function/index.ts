import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ValueAsFunction from './ValueAsFunction';
import ClientProvider from './client';

export default createDemo(import.meta.url, ValueAsFunction, { ClientProvider });
