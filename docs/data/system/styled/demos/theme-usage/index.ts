import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ThemeUsage from './ThemeUsage';
import ClientProvider from './client';

export default createDemo(import.meta.url, ThemeUsage, { ClientProvider });
