import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import StyledComponentsTheme from './StyledComponentsTheme';

export default createDemo(import.meta.url, StyledComponentsTheme);
