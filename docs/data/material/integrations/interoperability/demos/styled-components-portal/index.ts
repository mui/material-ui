import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import StyledComponentsPortal from './StyledComponentsPortal';

export default createDemo(import.meta.url, StyledComponentsPortal);
