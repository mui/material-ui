import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LinkRouterWithTheme from './LinkRouterWithTheme';
import ClientProvider from './client';

export default createDemo(import.meta.url, LinkRouterWithTheme, { ClientProvider });
