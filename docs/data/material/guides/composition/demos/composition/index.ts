import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import Composition from './Composition';
import ClientProvider from './client';

export default createDemo(import.meta.url, Composition, { ClientProvider });
