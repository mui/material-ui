import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SimpleSlide from './SimpleSlide';
import ClientProvider from './client';

export default createDemo(import.meta.url, SimpleSlide, { ClientProvider });
