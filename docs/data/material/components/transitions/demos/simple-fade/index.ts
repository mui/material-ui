import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SimpleFade from './SimpleFade';
import ClientProvider from './client';

export default createDemo(import.meta.url, SimpleFade, { ClientProvider });
