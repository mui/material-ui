import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import Elevation from './Elevation';
import ClientProvider from './client';

export default createDemo(import.meta.url, Elevation, { ClientProvider });
