import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import GroupedMenu from './GroupedMenu';
import ClientProvider from './client';

export default createDemo(import.meta.url, GroupedMenu, { ClientProvider });
