import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import AccountMenu from './AccountMenu';
import ClientProvider from './client';

export default createDemo(import.meta.url, AccountMenu, { ClientProvider });
