import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import DenseMenu from './DenseMenu';
import ClientProvider from './client';

export default createDemo(import.meta.url, DenseMenu, { ClientProvider });
