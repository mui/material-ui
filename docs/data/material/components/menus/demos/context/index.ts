import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ContextMenu from './ContextMenu';
import ClientProvider from './client';

export default createDemo(import.meta.url, ContextMenu, { ClientProvider });
