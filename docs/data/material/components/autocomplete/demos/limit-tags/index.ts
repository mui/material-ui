import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LimitTags from './LimitTags';
import ClientProvider from './client';

export default createDemo(import.meta.url, LimitTags, { ClientProvider });
