import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import RenderGroup from './RenderGroup';
import ClientProvider from './client';

export default createDemo(import.meta.url, RenderGroup, { ClientProvider });
