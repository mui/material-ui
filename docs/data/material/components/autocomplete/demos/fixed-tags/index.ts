import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FixedTags from './FixedTags';
import ClientProvider from './client';

export default createDemo(import.meta.url, FixedTags, { ClientProvider });
