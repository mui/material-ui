import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import GitHubLabel from './GitHubLabel';
import ClientProvider from './client';

export default createDemo(import.meta.url, GitHubLabel, { ClientProvider });
