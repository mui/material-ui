import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import Highlights from './Highlights';
import ClientProvider from './client';

export default createDemo(import.meta.url, Highlights, { ClientProvider });
