import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import CustomInputAutocomplete from './CustomInputAutocomplete';

export default createDemo(import.meta.url, CustomInputAutocomplete);
