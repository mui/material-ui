import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import MiniDrawer from './MiniDrawer';
import ClientProvider from './client';

export default createDemo(import.meta.url, MiniDrawer, { ClientProvider });
