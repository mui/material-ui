import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FlexDivider from './FlexDivider';
import ClientProvider from './client';

export default createDemo(import.meta.url, FlexDivider, { ClientProvider });
