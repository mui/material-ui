import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicChips from './BasicChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicChips, { ClientProvider });
