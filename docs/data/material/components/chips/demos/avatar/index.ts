import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import AvatarChips from './AvatarChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, AvatarChips, { ClientProvider });
