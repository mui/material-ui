import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ColorChips from './ColorChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, ColorChips, { ClientProvider });
