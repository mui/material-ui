import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SizesChips from './SizesChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, SizesChips, { ClientProvider });
