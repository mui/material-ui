import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import IconChips from './IconChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, IconChips, { ClientProvider });
