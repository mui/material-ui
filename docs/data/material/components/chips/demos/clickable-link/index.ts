import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ClickableLinkChips from './ClickableLinkChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, ClickableLinkChips, { ClientProvider });
