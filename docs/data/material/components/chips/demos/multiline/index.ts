import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import MultilineChips from './MultilineChips';
import ClientProvider from './client';

export default createDemo(import.meta.url, MultilineChips, { ClientProvider });
