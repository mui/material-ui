import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ChipsArray from './ChipsArray';
import ClientProvider from './client';

export default createDemo(import.meta.url, ChipsArray, { ClientProvider });
