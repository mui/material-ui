<Chip
  label="Assign"
  startAdornment={<PersonAddIcon />}
  action={<ChipButton onClick={() => {}} />}
/>
<Chip
  label="Reset"
  variant="outlined"
  color="primary"
  endAdornment={<LoopIcon />}
  action={<ChipButton onClick={() => {}} />}
/>