import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ErrorRadios from './ErrorRadios';
import ClientProvider from './client';

export default createDemo(import.meta.url, ErrorRadios, { ClientProvider });
