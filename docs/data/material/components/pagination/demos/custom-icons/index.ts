import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import CustomIcons from './CustomIcons';
import ClientProvider from './client';

export default createDemo(import.meta.url, CustomIcons, { ClientProvider });
