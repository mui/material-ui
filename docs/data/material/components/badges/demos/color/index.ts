import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ColorBadge from './ColorBadge';
import ClientProvider from './client';

export default createDemo(import.meta.url, ColorBadge, { ClientProvider });
