import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import TextButtons from './TextButtons';
import ClientProvider from './client';

export default createDemo(import.meta.url, TextButtons, { ClientProvider });
