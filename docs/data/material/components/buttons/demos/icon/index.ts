import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import IconButtons from './IconButtons';
import ClientProvider from './client';

export default createDemo(import.meta.url, IconButtons, { ClientProvider });
