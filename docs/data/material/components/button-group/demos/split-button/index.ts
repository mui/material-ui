import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SplitButton from './SplitButton';
import ClientProvider from './client';

export default createDemo(import.meta.url, SplitButton, { ClientProvider });
