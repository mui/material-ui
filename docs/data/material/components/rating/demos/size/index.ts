import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import RatingSize from './RatingSize';
import ClientProvider from './client';

export default createDemo(import.meta.url, RatingSize, { ClientProvider });
