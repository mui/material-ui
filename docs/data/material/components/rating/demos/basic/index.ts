import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicRating from './BasicRating';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicRating, { ClientProvider });
