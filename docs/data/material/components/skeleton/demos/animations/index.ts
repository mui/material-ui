import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import Animations from './Animations';
import ClientProvider from './client';

export default createDemo(import.meta.url, Animations, { ClientProvider });
