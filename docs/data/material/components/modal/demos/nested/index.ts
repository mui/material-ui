import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import NestedModal from './NestedModal';
import ClientProvider from './client';

export default createDemo(import.meta.url, NestedModal, { ClientProvider });
