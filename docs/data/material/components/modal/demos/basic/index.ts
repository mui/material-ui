import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicModal from './BasicModal';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicModal, { ClientProvider });
