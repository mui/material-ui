import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FormDialog from './FormDialog';
import ClientProvider from './client';

export default createDemo(import.meta.url, FormDialog, { ClientProvider });
