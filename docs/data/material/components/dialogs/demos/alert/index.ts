import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import AlertDialog from './AlertDialog';
import ClientProvider from './client';

export default createDemo(import.meta.url, AlertDialog, { ClientProvider });
