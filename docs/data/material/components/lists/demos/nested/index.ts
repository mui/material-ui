import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import NestedList from './NestedList';
import ClientProvider from './client';

export default createDemo(import.meta.url, NestedList, { ClientProvider });
