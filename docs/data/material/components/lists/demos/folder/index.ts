import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FolderList from './FolderList';
import ClientProvider from './client';

export default createDemo(import.meta.url, FolderList, { ClientProvider });
