import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicList from './BasicList';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicList, { ClientProvider });
