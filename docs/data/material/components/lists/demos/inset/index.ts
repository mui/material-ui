import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import InsetList from './InsetList';
import ClientProvider from './client';

export default createDemo(import.meta.url, InsetList, { ClientProvider });
