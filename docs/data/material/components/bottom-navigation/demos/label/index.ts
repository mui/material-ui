import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LabelBottomNavigation from './LabelBottomNavigation';

export default createDemo(import.meta.url, LabelBottomNavigation);
