import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FontAwesomeIconSize from './FontAwesomeIconSize';
import ClientProvider from './client';

export default createDemo(import.meta.url, FontAwesomeIconSize, { ClientProvider });
