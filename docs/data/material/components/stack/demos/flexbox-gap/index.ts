import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FlexboxGapStack from './FlexboxGapStack';
import ClientProvider from './client';

export default createDemo(import.meta.url, FlexboxGapStack, { ClientProvider });
