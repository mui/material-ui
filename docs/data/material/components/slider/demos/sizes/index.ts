import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SliderSizes from './SliderSizes';
import ClientProvider from './client';

export default createDemo(import.meta.url, SliderSizes, { ClientProvider });
