import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import NoOppositeContent from './NoOppositeContent';
import ClientProvider from './client';

export default createDemo(import.meta.url, NoOppositeContent, { ClientProvider });
