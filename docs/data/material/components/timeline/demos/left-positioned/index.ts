import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LeftPositionedTimeline from './LeftPositionedTimeline';

export default createDemo(import.meta.url, LeftPositionedTimeline);
