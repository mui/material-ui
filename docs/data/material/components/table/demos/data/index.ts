import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import DataTable from './DataTable';
import ClientProvider from './client';

export default createDemo(import.meta.url, DataTable, { ClientProvider });
