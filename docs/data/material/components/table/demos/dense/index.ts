import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import DenseTable from './DenseTable';
import ClientProvider from './client';

export default createDemo(import.meta.url, DenseTable, { ClientProvider });
