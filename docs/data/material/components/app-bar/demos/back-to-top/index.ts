import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BackToTop from './BackToTop';
import ClientProvider from './client';

export default createDemo(import.meta.url, BackToTop, { ClientProvider });
