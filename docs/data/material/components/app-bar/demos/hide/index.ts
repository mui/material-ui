import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import HideAppBar from './HideAppBar';
import ClientProvider from './client';

export default createDemo(import.meta.url, HideAppBar, { ClientProvider });
