import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import MenuAppBar from './MenuAppBar';
import ClientProvider from './client';

export default createDemo(import.meta.url, MenuAppBar, { ClientProvider });
