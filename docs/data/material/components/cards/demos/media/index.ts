import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import MediaCard from './MediaCard';
import ClientProvider from './client';

export default createDemo(import.meta.url, MediaCard, { ClientProvider });
