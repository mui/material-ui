import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LinearColor from './LinearColor';
import ClientProvider from './client';

export default createDemo(import.meta.url, LinearColor, { ClientProvider });
