import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import LinearQuery from './LinearQuery';
import ClientProvider from './client';

export default createDemo(import.meta.url, LinearQuery, { ClientProvider });
