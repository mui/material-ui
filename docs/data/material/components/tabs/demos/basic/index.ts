import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicTabs from './BasicTabs';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicTabs, { ClientProvider });
