import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import CustomizedInputsStyled from './CustomizedInputsStyled';
import ClientProvider from './client';

export default createDemo(import.meta.url, CustomizedInputsStyled, { ClientProvider });
