import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import CustomizedInputsStyleOverrides from './CustomizedInputsStyleOverrides';
import ClientProvider from './client';

export default createDemo(import.meta.url, CustomizedInputsStyleOverrides, { ClientProvider });
