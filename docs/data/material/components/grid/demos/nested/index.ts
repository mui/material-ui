import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import NestedGrid from './NestedGrid';
import ClientProvider from './client';

export default createDemo(import.meta.url, NestedGrid, { ClientProvider });
