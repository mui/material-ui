import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import NestedGridColumns from './NestedGridColumns';
import ClientProvider from './client';

export default createDemo(import.meta.url, NestedGridColumns, { ClientProvider });
