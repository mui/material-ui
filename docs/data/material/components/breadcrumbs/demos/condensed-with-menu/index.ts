import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import CondensedWithMenu from './CondensedWithMenu';
import ClientProvider from './client';

export default createDemo(import.meta.url, CondensedWithMenu, { ClientProvider });
