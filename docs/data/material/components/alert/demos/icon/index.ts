import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import IconAlerts from './IconAlerts';
import ClientProvider from './client';

export default createDemo(import.meta.url, IconAlerts, { ClientProvider });
