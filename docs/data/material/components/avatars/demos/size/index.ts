import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import SizeAvatars from './SizeAvatars';
import ClientProvider from './client';

export default createDemo(import.meta.url, SizeAvatars, { ClientProvider });
