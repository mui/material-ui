import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import IconAvatars from './IconAvatars';
import ClientProvider from './client';

export default createDemo(import.meta.url, IconAvatars, { ClientProvider });
