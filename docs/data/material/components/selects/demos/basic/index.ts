import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import BasicSelect from './BasicSelect';
import ClientProvider from './client';

export default createDemo(import.meta.url, BasicSelect, { ClientProvider });
