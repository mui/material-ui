import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import StatSlots from './StatSlots';
import ClientProvider from './client';

export default createDemo(import.meta.url, StatSlots, { ClientProvider });
