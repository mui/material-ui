import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ContrastThreshold from './ContrastThreshold';
import ClientProvider from './client';

export default createDemo(import.meta.url, ContrastThreshold, { ClientProvider });
