import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import HighContrastTokens from './HighContrastTokens';
import ClientProvider from './client';

export default createDemo(import.meta.url, HighContrastTokens, { ClientProvider });
