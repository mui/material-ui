import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import TonalOffset from './TonalOffset';
import ClientProvider from './client';

export default createDemo(import.meta.url, TonalOffset, { ClientProvider });
