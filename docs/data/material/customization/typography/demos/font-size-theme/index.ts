import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FontSizeTheme from './FontSizeTheme';
import ClientProvider from './client';

export default createDemo(import.meta.url, FontSizeTheme, { ClientProvider });
