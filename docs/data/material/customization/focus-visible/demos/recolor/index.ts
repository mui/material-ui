import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FocusVisibleRecolor from './FocusVisibleRecolor';
import ClientProvider from './client';

export default createDemo(import.meta.url, FocusVisibleRecolor, { ClientProvider });
