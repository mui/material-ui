import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FocusVisibleCustomIcons from './FocusVisibleCustomIcons';
import ClientProvider from './client';

export default createDemo(import.meta.url, FocusVisibleCustomIcons, { ClientProvider });
