import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FocusVisibleDefault from './FocusVisibleDefault';
import ClientProvider from './client';

export default createDemo(import.meta.url, FocusVisibleDefault, { ClientProvider });
