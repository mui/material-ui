import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FocusVisibleColoredSurface from './FocusVisibleColoredSurface';
import ClientProvider from './client';

export default createDemo(import.meta.url, FocusVisibleColoredSurface, { ClientProvider });
