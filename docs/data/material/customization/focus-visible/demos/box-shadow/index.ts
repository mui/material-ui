import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import FocusVisibleBoxShadow from './FocusVisibleBoxShadow';
import ClientProvider from './client';

export default createDemo(import.meta.url, FocusVisibleBoxShadow, { ClientProvider });
