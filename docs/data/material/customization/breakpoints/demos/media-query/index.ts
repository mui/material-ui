import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import MediaQuery from './MediaQuery';
import ClientProvider from './client';

export default createDemo(import.meta.url, MediaQuery, { ClientProvider });
