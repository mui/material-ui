import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import ContrastTextDemo from './ContrastTextDemo';
import ClientProvider from './client';

export default createDemo(import.meta.url, ContrastTextDemo, { ClientProvider });
