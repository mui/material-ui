import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import GlobalThemeOverrideSx from './GlobalThemeOverrideSx';
import ClientProvider from './client';

export default createDemo(import.meta.url, GlobalThemeOverrideSx, { ClientProvider });
