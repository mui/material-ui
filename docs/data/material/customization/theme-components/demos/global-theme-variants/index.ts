import { createDemo } from '@mui/internal-core-docs/utils/createDemo';

import GlobalThemeVariants from './GlobalThemeVariants';
import ClientProvider from './client';

export default createDemo(import.meta.url, GlobalThemeVariants, { ClientProvider });
