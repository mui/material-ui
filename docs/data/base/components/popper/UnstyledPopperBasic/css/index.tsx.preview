<React.Fragment>
  <button
    type="button"
    aria-describedby={id}
    className="Button"
    onClick={handleClick}
  >
    Toggle Popper
  </button>
  <Popper id={id} open={open} anchorEl={anchorEl}>
    <div className="CustomPopper">The content of the Popper.</div>
  </Popper>
  <Styles />
</React.Fragment>