<React.Fragment>
  <button type="button" onClick={handleOpen}>
    Open Snackbar
  </button>
  {open ? (
    <CustomSnackbar {...getRootProps()}>Hello World</CustomSnackbar>
  ) : null}
</React.Fragment>